<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN"
"http://www.oasis-open.org/docbook/xml/4.3/docbookx.dtd">
<section id="iso"
         xreflabel="iso">
  <title>
  ISO
  </title>

  <section>
    <title>
    Definition
    </title>

    <para>
    Creates ISO images of the system repository for installation via CD or DVD.
    </para>

    <para>
    Also used by Deploy during
    the creation of <emphasis>iso</emphasis> images. 
    When creating iso images, including the boot.iso image, Deploy
    appends provided options to each "append" line in the syslinux
    configuration file. In addition, iso images are automatically configured
    to use a kickstart, if provided. 
    </para>

  </section>

  <section>
    <title>
    Syntax
    </title>

    <programlisting>
[&lt;iso&gt;
   &lt;set&gt;TEXT&lt;/set&gt;*
 &lt;/iso&gt;]</programlisting>
  </section>

  <section>
    <title>
    Elements
    </title>

    <variablelist>
    <varlistentry>
      <term>
      set
      </term>

      <listitem>
      <para>
      <xref linkend="content-text"/> value specifying the size per ISO, in
      bytes, of a set of ISO images to create.  May include SI-style suffix
      ("MB", "GB", etc.).  The reserved values "CD" and "DVD" correspond to
      "640MB" and "4.7GB", respectively.
      </para>

      <para>
      This element must occur one or more times. Each instance will result
      in the generation of another set of ISO images.
      </para>

      <note>
      <para><emphasis>iso/set</emphasis> values are case- and
      space-insensitive, and the trailing 'b' is optional. For instance,
      the following <varname>TEXT</varname> values are all equivalent:
      "640000000", "640M", "640 mb", "CD".
      </para>

      <para>
      By default, sizes are computed using 1000 bytes per megabyte.
      In order to use 1024 bytes per megabyte, insert an 'i' after the
      order designator; for example, "640 MIB" or "4.7GiB".
      </para>
      <para>
      The iso element is ignored if <xref linkend="main-type"/> is set to
      "component" or "application".
      </para>
      </note>

      </listitem>
    </varlistentry>
    </variablelist>
  </section>

  <section>
    <title>
    Examples
    </title>

    <para>
    In the following example, <application class="software">Repo
    Studio</application> will create four sets of ISO images; one with 4.7
    gigabytes per disc ("DVD"), one with 400 megabytes per disc ("400000000"),
    one with 640 megabytes per disc ("640 mb"), and one with 640 mebibytes per
    disc ("640 mib"). Each set will be configured with
    <constant>keymap</constant> and <constant>lang</constant> boot arguments.
    </para>

    <informalexample>
    <programlisting>
&lt;iso&gt;
  &lt;set&gt;DVD&lt;/set&gt;       &lt;!-- 4700000000 bytes/disc --&gt;
  &lt;set&gt;400000000&lt;/set&gt; &lt;!--  400000000 bytes/disc --&gt;
  &lt;set&gt;640 mb&lt;/set&gt;    &lt;!--  640000000 bytes/disc --&gt;
  &lt;set&gt;640 mib&lt;/set&gt;   &lt;!--  671088640 bytes/disc --&gt;
&lt;/iso&gt;</programlisting>
    </informalexample>
  </section>

</section>
