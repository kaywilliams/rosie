<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN"
"http://www.oasis-open.org/docbook/xml/4.3/docbookx.dtd">
<section id="main" xreflabel="main">
  <title>
  Main (required)
  </title>

  <section>
    <title>
    Definition
    </title>

    <para>Contains basic information about the solution.</para>

  </section>

  <section>
    <title>
    Syntax
    </title>

    <programlisting>
&lt;main&gt;
  &lt;name&gt;TEXT&lt;/name&gt;
  &lt;version&gt;("5"|"6")&lt;/version&gt;
  &lt;arch&gt;("i386"|"x86_64")&lt;/arch&gt;
 [&lt;fullname&gt;TEXT&lt;/fullname&gt;]
 [&lt;id&gt;TEXT&lt;/id&gt;]
 [&lt;bug-url&gt;TEXT&lt;/bug-url&gt;]
 [&lt;log-file&gt;PATH&lt;/log-file&gt;]
&lt;/main&gt;
    </programlisting>
  </section>

  <section>
    <title>
    Elements
    </title>

    <variablelist>
    <varlistentry id="main-name"
                  xreflabel="name">
      <term>
      name
      </term>

      <listitem>
      <para>
      The name of the solution.
      </para>

      <para>
      The text value of this element can be accessed within other elements 
      and attributes of the definition using the builtin macro 
      <varname>%{name}</varname>.
      </para>

      <para>
      This element is required.
      </para>
      </listitem>
    </varlistentry>

    <varlistentry id="main-version"
                  xreflabel="version">
      <term>
      version
      </term>

      <listitem>
      <para>
      The version of the base operating system for the solution. Valid values
      are "5" and "6". 
      </para>

      <para>
      The text value of this element can be accessed within other elements 
      and attributes of the definition using the builtin macro 
      <varname>%{version}</varname>.
      </para>

      <para>
      This element is required.
      </para>
      </listitem>
    </varlistentry>

    <varlistentry id="main-arch"
                  xreflabel="arch">
      <term>
      arch
      </term>

      <listitem>
      <para>
      The architecture of the base operating system for the solution.
      Accepts either "i386" or "x86_64".</para>

      <para>
      The text value of this element can be accessed within other elements 
      and attributes of the definition using the builtin macro 
      <varname>%{arch}</varname>.
      </para>

      <para>
      This element is required.
      </para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term>
      fullname
      </term>

      <listitem>
      <para>
      A human-friendly name for the solution. Displays during client machine
      installation.
      </para>

      <para>
      The default value is <varname>%{name}</varname>.
      </para>
      </listitem>
    </varlistentry>

    <varlistentry id="main-id"
                  xreflabel="id">
      <term>
      id
      </term>

      <listitem>
      <para>
      The unique id for the solution. Used as the directory name
      for the solution in the CentOS Studio publish and cache folders
      (by default <emphasis>/var/www/html/solutions/</emphasis> and 
      <emphasis>/var/cache/centosstudio</emphasis>, respectively).
      </para>

      <para>
      The default value is <varname>%{name}</varname>-<varname>%{version}</varname>-<varname>%{arch}</varname>.
      </para>

      <para>
      The text value of this element can be accessed within other elements and
      attributed of the definition using the builtin macro 
      <varname>%{id}</varname>.
      </para>

      </listitem>
    </varlistentry>

    <varlistentry>
      <term>
      log-file
      </term>

      <listitem>
      <para>
      The <varname>PATH</varname> to the solution’s build log file.
      </para>

      <para>
      The default value is <filename>/var/log/centosstudio.log</filename>.
      </para>
      </listitem>
    </varlistentry>

    </variablelist>
  </section>

  <section>
    <title>
    Examples
    </title>

    <informalexample>
    <programlisting>
&lt;main&gt;
  &lt;name&gt;samba-server&lt;/name&gt;
  &lt;version&gt;5&lt;/version&gt;
  &lt;arch&gt;i386&lt;/arch&gt;
  &lt;fullname&gt;CentOS Samba Server&lt;/fullname&gt;
  &lt;bug-url&gt;http://www.example.com/bugs&lt;/bug-url&gt;
  &lt;log-file&gt;/home/centosstudiouser/SPIN/eg_CentOS5_main.log&lt;/log-file&gt;
&lt;/main&gt;
    </programlisting>
    </informalexample>
  </section>

  <section>
    <title>
    See Also
    </title>

    <para>
    <varname><xref linkend="content-dirname" /></varname>,
    <varname><xref linkend="content-path" /></varname>,
    <varname><xref linkend="content-text" /></varname>
    </para>
  </section>
</section>
