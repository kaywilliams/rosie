<?xml version="1.0" encoding="utf-8"?>
<rpm id='avahi'>
  <requires>avahi</requires>
  <script type='post'>
  # copy sftp-ssh.service script into the avahi folder
  /bin/cp -f /usr/share/doc/avahi-*/sftp-ssh.service /etc/avahi/services
    
  # restart avahi service
  if [[ ! `ps aux | grep "[/]usr/bin/anaconda"` ]]; then
    service avahi-daemon restart 
  fi
  </script>
</rpm>
