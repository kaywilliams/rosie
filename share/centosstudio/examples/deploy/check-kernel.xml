<?xml version="1.0" encoding="utf-8"?>
<script id='check-kernel' type='post' ssh='true'>
#!/bin/bash
if [[ `uname -r` != `rpm -q kernel | sed s/kernel-//` ]] ; then
  echo "running kernel differs from installed - restarting machine"
  shutdown -r now
fi
</script>
