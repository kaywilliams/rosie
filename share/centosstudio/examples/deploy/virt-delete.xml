<?xml version="1.0" encoding="utf-8"?>
<script id='virt-delete' type='delete'>
#!/bin/bash
set -e
if [[ `virsh list` = *\ %{hostname}%{domain}\ * ]]; then virsh destroy %{hostname}%{domain}; fi
if [[ `virsh list --all` = *\ %{hostname}%{domain}\ * ]]; then virsh undefine %{hostname}%{domain}; fi
</script>
