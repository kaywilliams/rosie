EXAMPLE_FILES = $(wildcard *)

.PHONY: all clean install

all:

install:
	mkdir -p $(DESTDIR)/usr/share/centosstudio/templates
	cp -var $(EXAMPLE_FILES) $(DESTDIR)/usr/share/centosstudio/templates

clean:
