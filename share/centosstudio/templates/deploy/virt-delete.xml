<?xml version="1.0" encoding="utf-8"?>
<script id='virt-delete' type='delete'>
#!/bin/bash
set -e
if [[ `virsh list` = *\ %{fqdn}\ * ]]; then
  virsh managedsave-remove %{fqdn}
  virsh destroy %{fqdn}
fi
if [[ `virsh list --all` = *\ %{fqdn}\ * ]]; then virsh undefine %{fqdn}; fi
</script>
