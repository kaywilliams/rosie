<?xml version="1.0" encoding="UTF-8"?>
<config-rpm id='azure-cli-config' xmlns:xi="http://www.w3.org/2001/XInclude">

<!-- include epel repo here or in your containing definition. 
     see http://mirrors.fedoraproject.org/publiclist/EPEL for a list of 
     public mirrors
<repo id='epel'>
<macro id='epel-mirror'>http://my.epel.mirror/epel</macro>
<baseurl>%{epel-mirror}/%{version}/%{arch}</baseurl>
<gpgkey>%{epel-mirror}/RPM-GPG-KEY-EPEL-%{version}</gpgkey>
</repo>
-->

<requires>nodejs</requires>
<requires>npm</requires>
<requires>python-simplejson</requires>

<script type='post'>
# todo, display stdout if error
npm install azure-cli -g >/dev/null
</script>
</config-rpm>
