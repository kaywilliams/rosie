<?xml version="1.0" encoding="utf-8"?>
<script xmlns:xi="http://www.w3.org/2001/XInclude"
        id='test-deploy-developer' type='update'>
#!/bin/sh
set -e

# update deploy sources
cd %{mercurial-workspace}/deploy
hg pull -u

# test deploy operation
deploy share/deploy/templates/%{norm-os}/httpd/web-server.definition \
  --macro os:%{os} \
  --list-events \
  --debug
</script>
